{
  "name": "orcajs",
  "version": "1.1.3",
  "main": "./dist/orca.js",
  "license": "MIT",
  "description": "orchestrate your javascript",
  "scripts": {
    "build": "babel src --out-dir dist",
    "test": "isparta cover _mocha",
    "coveralls": "npm run test -- --reporter locovonly && cat ./coverage/lcov.info | coveralls",
    "prepare": "npm run build"
  },
  "dependencies": {
    "lodash": "^4.17.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.23.1",
    "coveralls": "3.0.0",
    "isparta": "^4.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^4.0.1",
    "mocha-lcov-reporter": "^1.3.0",
    "simple-mock": "^0.8.0"
  }
}
